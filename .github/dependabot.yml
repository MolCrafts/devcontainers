version: 2
updates:
  # The workflows pin third-party actions by major tag; dependabot is what
  # notices when a major moves (or when one is deprecated by GitHub).
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
    labels:
      - dependencies
